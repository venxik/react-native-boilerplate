import * as components from '../../src/theme/components';

describe('Components Native Base', () => {
  it('Button', () => {
    expect(components).toBeTruthy();
  });
  // it('Input', () => {
  //   expect(components.Input).toBeTruthy();
  // });
  // it('Progress', () => {
  //   expect(components.Progress).toBeTruthy();
  // });
  // it('Select', () => {
  //   expect(components.Select).toBeTruthy();
  // });
  // it('Slider', () => {
  //   expect(components.Slider).toBeTruthy();
  // });
});
